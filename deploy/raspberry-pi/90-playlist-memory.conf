# Pi Zero 2 W: retain headroom for Wi-Fi packet buffers during NAS reads.
# These are workload-specific values for the 512 MB DietPi deployment.
# Test under load before applying to another machine.
vm.min_free_kbytes = 16384
vm.watermark_scale_factor = 100
