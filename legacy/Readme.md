# Archived prototype

These files preserve the original whole-recording implementation and its old
installation instructions. They are not part of the active package or Docker
image. Their dependency pins and API usage are historical, not a supported setup.

Local old test output and temporary files are preserved in the Git-ignored
`artifacts/` directory. Use the root README and `playlist_extractor/` for current work.
